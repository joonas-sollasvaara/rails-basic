<h1>Books</h1>

<ul>
  <% @books.each do |book| %>
    <li>
      <strong>
        <%= link_to book.title, book_path(book) %>
      </strong>
      by <%= book.authors %>
    </li>
  <% end %>
</ul>

<%= link_to "New book", new_book_path %>